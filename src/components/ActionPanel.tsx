import { ActionId, GameState } from "@/types/game"; import { actions } from "@/data/actions"; import { getVillage } from "@/data/villages";
export default function ActionPanel({ state, onAction }: { state: GameState; onAction: (id: ActionId) => void }) { const locked = state.villageId === "lumir" && state.log.length < 4; return <section className="panel action-panel"><div className="section-heading"><div><div className="eyebrow">NEXT MOVE</div><h3>무엇을 할까요?</h3></div><span className="location-pill">{getVillage(state.villageId).icon} {getVillage(state.villageId).name}</span></div><div className="action-grid">{actions.map((action) => { const isTravelLocked = action.id === "travel" && locked; return <button key={action.id} disabled={isTravelLocked} className="action-card" onClick={() => onAction(action.id)} style={{ "--action-color": action.color } as React.CSSProperties}><span className="action-icon">{action.icon}</span><span><b>{action.name}</b><small>{isTravelLocked ? "조금 더 마을을 살펴보세요" : action.description}</small></span><strong>→</strong></button> })}</div><div className="hint">TIP · 행동은 용맥에 기억으로 남습니다. 같은 행동도 숙련도가 오를수록 더 안전해집니다.</div></section> }
