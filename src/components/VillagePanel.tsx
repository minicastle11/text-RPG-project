import { GameState } from "@/types/game"; import { villages } from "@/data/villages";
export default function VillagePanel({ state }: { state: GameState }) { return <section className="panel village-panel"><div className="eyebrow">KNOWN VILLAGES</div><h3>용맥 지도</h3><div className="village-list">{villages.map((village) => <div className={`village-item ${state.villageId === village.id ? "active" : ""}`} key={village.id}><span className="village-icon" style={{ color: village.accent }}>{village.icon}</span><div><b>{village.name}</b><small>{village.region}</small></div>{state.villageId === village.id ? <i>현재</i> : <i className="muted">기록됨</i>}</div>)}</div><div className="dragon-card"><span>◉</span><div><b>염룡 라그하트</b><small>관심도 {state.dragonAffinity} / 100</small><div className="bar flame"><i style={{ width: `${state.dragonAffinity}%` }} /></div></div>{state.dragonUnlocked && <em>해금</em>}</div>{!state.dragonUnlocked && <p className="unlock-note">관심도 80 달성 시 <strong>염룡의 전직</strong>이 해금됩니다.</p>}</section> }
