import { LogEntry } from "@/types/game";
export default function LogPanel({ logs }: { logs: LogEntry[] }) { return <section className="panel log-panel"><div className="section-heading"><div><div className="eyebrow">MEMORY STREAM</div><h3>기록의 흐름</h3></div><span className="live-dot">● LIVE</span></div><div className="logs">{logs.map((log) => <div className={`log-item ${log.type}`} key={log.id}><span className="log-mark">{log.type === "reward" ? "✦" : log.type === "travel" ? "➜" : log.type === "warning" ? "!" : "·"}</span><p>{log.text}<small>{log.time}</small></p></div>)}</div></section> }
